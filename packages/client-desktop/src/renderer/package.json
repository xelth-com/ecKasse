{
   "name": "@eckasse/renderer-ui",
   "version": "0.1.0",
   "private": true,
   "dependencies": {
      "uuid": "^10.0.0"
   },
   "scripts": {
      "start": "echo 'Static files now served by backend server'",
      "build": "echo 'No build needed for plain HTML/JS/CSS'",
      "test": "echo 'No tests specified'"
   },
   "devDependencies": {
      "@types/uuid": "^10.0.0"
   }
}
